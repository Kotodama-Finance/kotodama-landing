# Kotodama Finance — https://kotodamafinance.com
#
# Everything is open, deliberately. Being legible to machines is a GOAL of this
# project, not a concession: it is the same reason the content lives in the HTML
# and is not injected by JavaScript.
#
# AI crawlers (GPTBot, ClaudeBot, CCBot, PerplexityBot, Google-Extended…) are
# NOT blocked. They do not need to be named one by one to be allowed — the
# User-agent: * below already covers them. They are mentioned here so that the
# omission is on the record as deliberate, and nobody adds them "just in case"
# by copying someone else's robots.txt.
#
# Open access means open access to whoever reads by machine, too.

User-agent: *
Allow: /

Sitemap: https://kotodamafinance.com/sitemap.xml
